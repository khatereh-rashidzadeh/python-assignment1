name = input("enter Student name: ")
last_name = input("enter Student last name: ")
grade = float(input("enter Student grade: "))

average = grade

if average >= 10:
    status = "Passed"
else:
    status = "Failed"

print(f"{name} {last_name} with an average grade of {average} in the field of ... is {status}.")
